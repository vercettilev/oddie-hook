node_modules/

# Generated by `npm run smoke` on every run.
card-sample.svg

# Local env / secrets — no keys are needed today, but don't let one slip in later.
.env
.env.*

# Build output, if tsc ever emits.
dist/

# macOS
.DS_Store

# Rendered card previews (local only)
cards/
