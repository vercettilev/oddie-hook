# The app is open. What stays out of the index is the operator surface, which
# was never meant to be found, and the preview and roster pages. Market pages
# and the campaign ARE the share targets and must stay indexable.
User-agent: *
Disallow: /app/
Disallow: /preview
Disallow: /tool
Disallow: /genesis/roster
Allow: /
